# User Instruction: Add Issue Template

State: closed
Source: user instruction

## Context

The user requested a reusable issue template for tracking instructions, work units, reviewer findings, and design decisions.

## Impact

Without a template, issue records can drift in structure and omit state, source, fix, or verification details.

## Proposed Fix

Add a Markdown template under `docs/issues/` and update contributor guidance to require it.

## Fix Summary

Added `docs/issues/template.md` and updated `AGENTS.md` to reference it.

## Verification

Reviewed the template fields against the required issue-management workflow.
